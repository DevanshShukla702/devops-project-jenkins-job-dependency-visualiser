# FlowTrace Configuration (non-sensitive defaults)
# -----------------------------------------------
# Sensitive values (credentials, keys) are loaded
# from environment variables. See .env.example.
# -----------------------------------------------

app:
  title: "FlowTrace"
  demo_mode: false
  auto_refresh_interval: 30

auth:
  enabled: true

jenkins:
  url: "http://localhost:8080"
  username: ""
  token: ""
